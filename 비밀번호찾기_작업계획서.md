# 비밀번호 찾기(재설정) 기능 작업 계획서

- 작성일: 2026-06-23
- 대상 모듈: `vims-login`(프론트/인증 백엔드), `Core-lib`(공통 유틸), `vims-management-system`(내정보/공통코드 참조)
- 제약: **상용 라이브러리 도입 금지**, **기존 코딩 스타일 변경 금지**(기존 `formUtil`/`axios`/`Message.Label.Array`/서비스-매퍼 패턴 그대로 사용)

---

## 1. 목표 흐름

```
[로그인 화면] 비밀번호 찾기 클릭
  → 슬라이드 전환(기존 #sliding-reset-container 유지)
  → 아이디(이메일) 입력 → "인증코드 전송"
  → 서버가 6자리 코드 생성 → 대표이메일(PUBLIC_EMAIL_ID) 발신으로 해당 이메일에 발송
  → 화면 3분(180초) 카운트다운 시작
  → 3분 내 6자리 코드 입력 → "인증 확인"(서버 검증)
  → 검증 성공 시 단기 재설정 토큰 발급
  → 전용 비밀번호 재설정 페이지로 이동(내정보 수정 페이지와 동일 레이아웃)
  → 재설정 팝업: 새 비밀번호 / 새 비밀번호 확인
  → 정책 통과 + 일치 → 비밀번호 갱신
  → 로그인 화면으로 이동
```

### 확정된 설계 결정(사용자 확인 완료)

1. **코드 생성·검증은 전부 서버에서 처리**(현재 JS 생성·비교 스캐폴드는 보안 취약 → 폐기).
2. **전용 재설정 페이지** 사용(인증이 필요한 실제 내정보 페이지로 보내지 않고, 동일 레이아웃의 비인증 전용 화면 + 단기 토큰으로만 접근 허용).
3. **PUBLIC_EMAIL_ID(대표이메일)** 는 (a) 메일 발신(from) 주소 겸 (b) 메일 본문 내 문의처 안내로 **둘 다** 사용.

---

## 2. 현재 상태 분석

| 구성요소 | 상태 | 비고 |
|---|---|---|
| 로그인 슬라이드 전환(`#find-pw-btn` → `#sliding-reset-container`) | **존재** | `login.html` init/clickEvent에 구현됨, 유지 |
| 3분 카운트다운 타이머(`startCountdownTimer(180,...)`) | **존재** | 재사용 |
| 인증 메일 발송 함수(`sendAuthenticationEmail`) | **프론트만 존재** | `/common/mail/sendMail` 호출하나 **백엔드 미구현** |
| 6자리 코드 생성/검증 | **프론트(JS) 임시** | `generateRandomNumber()` 클라이언트 생성·비교 → **서버로 이관** |
| 재설정 폼 HTML(`loginPagePasswordInitializeForm`) | **빈 껍데기** | id/name/label 미작성 placeholder 상태 |
| 비밀번호 재설정 API(`initializePassword`, `validatePasswordPolicy`) | **미구현** | login.html이 호출만 함 |
| 내정보 비밀번호 변경 팝업(`change-password-popup`) | **존재(기존 PW 요구)** | 찾기 흐름엔 부적합 → 전용 팝업 신설 |
| 비밀번호 정책(`PWD_POLICY` site config) + `PasswordValidationUtil` | **존재** | 재사용 |
| 공통코드 조회(`/cms/common/sysCode/findSysCode`) | **존재** | `PUBLIC_EMAIL_ID` 조회에 사용 |
| `spring-boot-starter-mail` 의존성 | **존재(미사용)** | 구현만 필요(상용 라이브러리 아님, 제약 충족) |
| SMTP 설정(`spring.mail.*`) | **부재** | `application.yml`에 추가 필요 |
| 메일 발송 Java 코드(`JavaMailSender`) | **부재** | 신규 구현 |
| 보안 화이트리스트(`WHITE_LIST_URL`) | `/`, `/login`, `/common/**`, `/assets/**`, `/api/v1/auth/**` | 신규 엔드포인트는 이 경로 하위에 두면 비인증 접근 가능 |

---

## 3. 아키텍처 결정

### 3.1 엔드포인트 위치 / 라우팅
- 게이트웨이 라우팅: `/common/**`, `/api/v1/auth/**` → `vims-login(8081)`, `/cms/**` → `management(8083, 인증필요)`.
- 비밀번호 찾기는 **로그인 전(비인증)** 흐름이므로 **모든 신규 API를 `vims-login`에 두고 `/api/v1/auth/password/**` 경로로 노출**(이미 화이트리스트).
  - `/cms/**`(인증영역)를 익명에 개방하지 않아 보안 영향 최소화.
- `vims-login`은 이미 `mybatis`/`data-jpa`/`mariadb`/`Core-lib`(auth, `PasswordValidationUtil`) 보유 → DB 접근·정책검증·암호화 가능.

### 3.2 코드 저장 방식
- 서버 메모리 캐시(`ConcurrentHashMap<email, VerificationEntry>`)에 `{code, expireAt(3분), attempts}` 저장.
  - 외부 캐시/Redis 미도입(상용·인프라 추가 회피, 단일 인스턴스 기준). 다중 인스턴스 운영 시 별도 검토 필요(→ 5. 리스크).
- 검증 성공 시 별도 맵에 **단기 재설정 토큰**(UUID, 5분 만료, 1회용) 저장 → 재설정 API에서 검증.

### 3.3 비밀번호 정책/암호화
- 정책: 기존 `PWD_POLICY` site config + `Core-lib` `PasswordValidationUtil` 재사용(서버 검증).
- 암호화: 로그인과 동일한 `PasswordEncoder`(BCrypt) 빈 재사용. `sys_user.pswd` 갱신(이메일 필드 `eml` 기준 조회).

### 3.4 대표이메일(PUBLIC_EMAIL_ID)
- 공통코드에서 값 조회 후: 메일 `from` 헤더 + 본문 문의처로 사용.
- 주의: SMTP 인증 계정과 `from` 주소가 다르면 발송 거부/스팸 처리될 수 있음 → PUBLIC_EMAIL_ID는 **SMTP 인증 계정과 동일하거나 허용된 별칭**이어야 함(운영 SMTP 정책 확인 필요).

---

## 4. 작업 항목 (Phase별)

### Phase 1 — 백엔드: 메일 인프라
1. `vims-login/application.yml`에 `spring.mail.*`(host/port/username/password/properties) 추가. 비밀번호는 환경변수 주입(`${MAIL_PASSWORD:...}`) — 기존 DB 설정 스타일과 동일.
2. `MailService`(JavaMailSender 래퍼) 신설: HTML 메일 발송, `from`에 PUBLIC_EMAIL_ID 적용. 발송은 가급적 비동기/예외 격리.
3. 공통코드 조회 유틸: `PUBLIC_EMAIL_ID` 값을 서버에서 조회(Core-lib 또는 vims-login 매퍼). 프론트 노출 없이 서버 내부에서 from 구성.

### Phase 2 — 백엔드: 인증코드 발급·검증
4. `PasswordResetService` 신설:
   - `requestCode(email)`: 사용자 존재 확인 → 6자리 코드 생성 → 캐시 저장(3분) → `MailService`로 발송.
   - `verifyCode(email, code)`: 만료/시도횟수/일치 검증 → 성공 시 단기 재설정 토큰 발급·반환.
   - `resetPassword(token, newPwd, confirmPwd)`: 토큰 검증(만료/1회용) → 정책검증 → 암호화 → `sys_user` 갱신 → 토큰 폐기.
5. `PasswordResetController`(`@RequestMapping("/api/v1/auth/password")`):
   - `POST /code/request` `{email}`
   - `POST /code/verify` `{email, code}` → `{resetToken}`
   - `POST /reset` `{resetToken, newPassword, newPasswordConfirm}`
   - 응답/예외는 기존 `CustomException` + `Message`(EXCEPTION.*) 패턴 준수.
6. (선택) 정책 노출용 `GET /policy` 또는 기존 site config 조회 재사용(팝업에 정책 안내 표시용).

### Phase 3 — 프론트: 로그인 화면 인증 단계
7. `login.html`의 빈 재설정 폼(`loginPagePasswordInitializeForm`) → **인증코드 입력 폼**으로 실제 마크업 작성:
   - 이메일 입력 + "인증코드 전송" 버튼(`send-number-btn`)
   - 6자리 코드 입력 + "인증 확인" 버튼(`number-check-btn`)
   - 타이머(`.authentication-timer`)/메시지(`.authentication-check-message`) 영역
   - 기존 클래스/`data-field`/`Message.Label.Array` 스타일 그대로 사용.
8. JS 수정(기존 함수 스타일 유지):
   - `sendAuthenticationEmail()` → 서버 `POST /code/request` 호출로 교체(클라 코드 생성 제거), 성공 시 3분 타이머 시작.
   - 코드 검증 → `POST /code/verify` 호출, 성공 시 반환된 `resetToken`을 세션/메모리에 저장하고 전용 재설정 페이지로 이동.
   - `generateRandomNumber()`/클라 비교 로직 제거, 주석 처리된 `number-check-btn` 로직 정리.

### Phase 4 — 프론트: 전용 재설정 페이지 + 팝업
9. 전용 재설정 페이지 신설(내정보 레이아웃/`MyInfoPage.css` 재사용, 비인증 접근 가능 라우트). 진입 시 `resetToken` 유효성 확인.
10. 비밀번호 재설정 팝업: **새 비밀번호 / 새 비밀번호 확인**만 입력(기존 PW 미요구). 정책 안내 표시(`setPasswordPolicyInPopupSection` 패턴 재사용).
11. "확인" → `POST /reset` 호출 → 성공 시 안내 후 로그인 화면으로 이동(`requestPageRedirect` 패턴).

### Phase 5 — 보안/설정/메시지
12. 전용 재설정 페이지 라우트를 화이트리스트(`/api/v1/auth/**` 하위 또는 `/common/**`)에 포함, 단 실제 재설정은 **resetToken 검증으로만** 허용.
13. `Message.js`/`Message.en.js`/`Message.mn.js`에 신규 라벨 추가(LOGIN.* 다국어). 기존 LOGIN.* 라벨 최대 재사용.
14. 레이트리밋(이메일당 발송 간격/일일 횟수), 코드 시도횟수 제한(예: 5회) 적용.

### Phase 6 — 테스트/검증
15. 단위: 코드 생성·만료·시도횟수·토큰 1회용/만료, 정책검증 경로.
16. 통합: 발송→검증→재설정→로그인 E2E(정상/만료/오입력/토큰 위변조/존재하지 않는 이메일).
17. 메일 발송 실제 SMTP 점검(from=PUBLIC_EMAIL_ID 수신·스팸 여부).

---

## 5. 신규/수정 파일 목록(예상)

**백엔드(신규)**
- `vims-login/.../mail/MailService.java`
- `vims-login/.../password/PasswordResetService.java`
- `vims-login/.../password/PasswordResetController.java`
- `vims-login/.../password/dto/*` (요청/응답 DTO)
- (공통코드 조회용) `vims-login` 매퍼 또는 `Core-lib` 재사용

**백엔드(수정)**
- `vims-login/src/main/resources/application.yml` (spring.mail.*)
- `Core-lib/.../auth/config/SecurityConfiguration.java` (필요 시 재설정 페이지 경로 화이트리스트 확인)

**프론트(수정/신규)**
- `vims-login/.../templates/login/login.html` (인증 폼 마크업 + JS 서버연동)
- 전용 재설정 페이지 템플릿(신규) + 재설정 팝업
- `vims-login/.../static/common/js/common/Message.js` / `Message.en.js` / `Message.mn.js` (라벨)

---

## 6. API 명세(요약)

| 메서드/경로 | 요청 | 응답 | 비고 |
|---|---|---|---|
| `POST /api/v1/auth/password/code/request` | `{email}` | 200 / 에러 | 사용자 확인 후 코드 발송, 3분 유효 |
| `POST /api/v1/auth/password/code/verify` | `{email, code}` | `{resetToken}` | 시도횟수·만료 검증 |
| `POST /api/v1/auth/password/reset` | `{resetToken, newPassword, newPasswordConfirm}` | 200 / 에러 | 토큰·정책 검증 후 갱신 |

> 사용자 열거 방지를 위해 존재하지 않는 이메일도 동일/모호한 메시지 처리 여부는 운영정책에 맞춰 결정(→ 리스크).

---

## 7. 보안 고려사항
- 코드/토큰 **서버 보관·검증**, 6자리 코드 3분 만료, 검증 시도 횟수 제한.
- 재설정 토큰 1회용·단기 만료, 재설정 API는 토큰 없이는 거부.
- 발송 레이트리밋(스팸/폭주 방지), 로깅 시 코드·토큰 평문 미기록.
- `/cms/**`(인증영역) 비개방 유지.

## 8. 리스크 / 추가 확인 필요
- **SMTP 운영 정보**: host/port/계정, PUBLIC_EMAIL_ID가 발신 계정과 일치/별칭 허용되는지.
- **다중 인스턴스**: 인메모리 캐시는 단일 인스턴스 기준. 스케일아웃 시 공유 저장소 필요(상용 회피 시 DB 테이블 검토).
- **사용자 열거 방지** 메시지 정책.
- 전용 재설정 페이지의 비인증 라우팅을 게이트웨이/시큐리티 양쪽에서 정확히 허용했는지.

## 9. 권장 진행 순서
Phase 1 → 2(백엔드 완성·단위테스트) → 3 → 4(프론트 연동) → 5(보안/메시지) → 6(E2E). 백엔드 우선 완성 후 프론트가 실제 API에 연동.
