# 운영 배포 스크립트 설계서 (deploy-to-prod.sh)

> 작성일: 2026-06-26
> 목적: 각 모듈의 빌드 산출물(.jar)을 운영 서버의 대응 컨테이너에 올리고 재기동하는 스크립트의 설계 정의

---

## 1. 개요

각 모듈을 빌드하여 생성된 `.jar` 파일을, 운영 서버에서 동작 중인 **각 모듈 전용 컨테이너**에 복사하고, 컨테이너 내부의 Java 프로세스를 교체(재기동)하는 자동화 스크립트를 작성한다.

- **실행 위치**: 개발 PC(이 저장소가 있는 Mac) 또는 운영 서버
- **대상 컨테이너 엔진**: Podman (명령 호환을 위해 `docker`도 옵션으로 지원)
- **배포 단위**: 모듈별 독립 배포 가능 (전체 / 일부 선택)

---

## 2. 운영 환경 현황

운영 서버의 컨테이너는 모두 `eclipse-temurin:17-jdk` 이미지에 진입점이 `tail -f /dev/null`로 설정되어 있다. 즉 **컨테이너 자체는 idle 상태**이며, 실제 애플리케이션은 컨테이너 내부에서 `java -jar`로 별도 기동되는 구조다.

| 모듈(로컬 디렉터리)        | 컨테이너명         | 포트 매핑 (host→container) | 내부 포트 | 배포 JAR 파일명                  |
| -------------------------- | ------------------ | -------------------------- | --------- | -------------------------------- |
| `vims-login`               | `vims-login`       | 9081 → 8081                | 8081      | `vims-login-podman-ROOT.jar`     |
| `vims-management-system`   | `vims-management`  | 9082 → 8082                | 8082      | `vims-management-podman-ROOT.jar`|
| `FMS`                      | `vims-fms`         | 9083 → 8083                | 8083      | `FMS-podman-ROOT.jar`            |
| `vims-web-app`             | `vims-web-app`     | 9084 → 8084                | 8084      | `vims-web-app-podman-ROOT.jar`   |
| `vims-gateway`             | `vims-gateway`     | 9000 → 8080                | 8080      | `vims-gateway-podman-ROOT.jar`   |

> ⚠️ `vims-inspection`, `vims-register`는 아직 운영 컨테이너가 없으므로 이번 배포 대상에서 제외한다. 컨테이너 추가 시 모듈 테이블에 한 줄만 추가하면 되도록 설계한다.

### 중요 포인트
- **재기동의 의미**: 컨테이너의 PID 1은 `tail`이므로, `podman restart`로 컨테이너를 재시작해도 Java는 자동 기동되지 않는다. 따라서 **"재기동" = 컨테이너 내부에서 실행 중인 java 프로세스를 죽이고 새 JAR로 다시 실행**하는 것을 의미한다. (컨테이너 자체는 재시작하지 않는다)

---

## 3. 어떤 JAR을 배포할 것인가 (핵심 결정 사항)

빌드 결과물은 두 종류가 존재한다.

| 종류 | 위치 | 적용 설정 | 용도 |
| ---- | ---- | --------- | ---- |
| `*-ROOT.jar`         | `<모듈>/target/`  | `application.yml`        | 로컬/개발 환경 |
| `*-podman-ROOT.jar`  | `deploy-output/`  | `application-podman.yml` | **운영(컨테이너) 환경** |

운영 컨테이너는 서비스 간 통신과 DB 접속에 `host.containers.internal`을 사용하므로, **`application-podman.yml`이 반영된 `deploy-output/*-podman-ROOT.jar`를 배포해야 정상 동작**한다.

> 사용자가 "각 모듈의 target 폴더 아래 .jar"을 언급했지만, `target`에는 로컬 프로파일 JAR만 존재한다. 운영 배포에는 podman 프로파일 JAR이 필요하므로 아래 옵션 중 선택이 필요하다.

**옵션 A (권장):** `deploy-core-lib.sh`가 만들어 둔 `deploy-output/*-podman-ROOT.jar`를 배포한다.
**옵션 B:** `target/*-ROOT.jar`를 배포하되, 컨테이너 기동 시 `SPRING_PROFILES_ACTIVE=podman` 또는 외부 `application.yml` 마운트로 운영 설정을 주입한다.
**옵션 C:** 스크립트 내부에서 빌드까지 수행(`deploy-core-lib.sh` 호출) 후 옵션 A로 배포.

→ 본 설계서는 **옵션 A**를 기본으로 하고, `--source target|deploy-output`, `--build` 플래그로 옵션 B/C도 선택 가능하도록 한다.

---

## 4. 배포 흐름

```
[개발 PC]                                  [운영 서버]
   │                                          │
   │ 1. (옵션) 빌드: deploy-core-lib.sh        │
   │ 2. JAR 선택 (deploy-output 기본)          │
   │                                          │
   │ 3. scp JAR ──────────────────────────▶  스테이징 디렉터리
   │                                       (예: ~/vims-deploy/)
   │                                          │
   │ 4. ssh ──▶ podman cp JAR ───────────▶ 컨테이너 내부 /app/app.jar
   │ 5. ssh ──▶ 기존 java 프로세스 종료(pkill)  │
   │ 6. ssh ──▶ nohup java -jar 백그라운드 기동 │
   │ 7. ssh ──▶ 포트 헬스체크 (curl/응답확인)   │
   │                                          │
   │ 8. 결과 요약 출력                          │
```

### 단계별 상세

1. **(선택) 빌드** — `--build` 지정 시 `deploy-core-lib.sh` 실행하여 최신 JAR 생성
2. **JAR 경로 확인** — 모듈 테이블 기준으로 배포할 JAR 존재 여부 검증 (없으면 해당 모듈 skip + 경고)
3. **전송(scp)** — JAR을 운영 서버 스테이징 디렉터리로 복사
4. **컨테이너 주입(podman cp)** — `podman cp <staging>/xxx.jar <container>:/app/app.jar`
5. **기존 프로세스 종료** — `podman exec <container> pkill -f 'java -jar'` (없으면 무시)
6. **신규 기동** — `podman exec -d <container> sh -c 'cd /app && nohup java -jar app.jar > /app/app.log 2>&1 &'`
7. **헬스체크** — **컨테이너 내부**에서 ① java 프로세스 생존(`pgrep`) ② 앱 내부 포트(예: 8081) LISTEN 여부를 확인 (최대 N초 대기). ※ 호스트 포트(9081 등)는 podman 이 컨테이너 기동만으로도 항상 listen 하므로 java 기동 보장이 안 됨 → 반드시 내부 포트로 확인
8. **요약** — 모듈별 성공/실패/스킵 결과 표 출력

---

## 5. 스크립트 구성

### 5.1 설정 변수 (상단에서 일괄 관리)

```bash
REMOTE_HOST="<운영서버 IP/호스트>"      # 필수
REMOTE_USER="<SSH 계정>"               # 필수
REMOTE_SSH_PORT="22"
ENGINE="podman"                        # podman | docker
REMOTE_STAGING_DIR="~/vims-deploy"     # JAR 임시 저장 경로
CONTAINER_APP_DIR="/app"               # 컨테이너 내부 JAR 배치 경로
CONTAINER_JAR_NAME="app.jar"           # 컨테이너 내부 통일 JAR 이름
JAR_SOURCE="deploy-output"             # deploy-output | target
HEALTH_TIMEOUT=60                      # 헬스체크 최대 대기(초)
```

### 5.2 모듈 정의 테이블

```bash
# 형식: "표시명|로컬디렉터리|컨테이너명|host포트|JAR파일명(deploy-output기준)"
MODULES=(
  "Login|vims-login|vims-login|9081|vims-login-podman-ROOT.jar"
  "Management|vims-management-system|vims-management|9082|vims-management-podman-ROOT.jar"
  "FMS|FMS|vims-fms|9083|FMS-podman-ROOT.jar"
  "WebApp|vims-web-app|vims-web-app|9084|vims-web-app-podman-ROOT.jar"
  "Gateway|vims-gateway|vims-gateway|9000|vims-gateway-podman-ROOT.jar"
)
```

### 5.3 주요 함수

| 함수 | 역할 |
| ---- | ---- |
| `resolve_jar()`      | source(target/deploy-output)에 따라 실제 JAR 경로 결정 및 존재 검증 |
| `upload_jar()`       | scp로 운영 서버 스테이징 디렉터리에 전송 |
| `deploy_container()` | podman cp → 기존 프로세스 종료 → 신규 기동 |
| `health_check()`     | 포트 LISTEN/HTTP 응답 확인 (타임아웃) |
| `deploy_module()`    | 위 함수들을 모듈 단위로 묶어 실행 + 결과 기록 |

### 5.4 실행 인터페이스(CLI)

```bash
# 전체 모듈 배포 (deploy-output의 podman JAR 사용)
./deploy-to-prod.sh

# 특정 모듈만 배포
./deploy-to-prod.sh login gateway

# 빌드부터 수행 후 배포
./deploy-to-prod.sh --build

# JAR 소스를 target으로 변경
./deploy-to-prod.sh --source target

# 재기동 없이 JAR 복사만 (점검용)
./deploy-to-prod.sh --no-restart
```

---

## 6. 안전장치 / 예외 처리

- **JAR 부재**: 해당 모듈 skip + 경고, 전체 중단하지 않음
- **SSH/scp 실패**: 모듈 단위 실패 처리, 나머지 모듈 계속 진행
- **롤백 대비**: 컨테이너에 기존 JAR을 `app.jar.bak`으로 백업 후 신규 복사 (실패 시 수동 복구 안내)
- **무중단 아님 명시**: 프로세스 종료~신규 기동 사이 짧은 다운타임 발생 (수 초~수십 초)
- **로그 위치**: 컨테이너 내부 `/app/app.log` (배포 후 `podman logs`가 아닌 해당 파일 확인)
- **기동 실패 감지**: 헬스체크 타임아웃 시 해당 모듈 FAIL로 표기하고 로그 마지막 N줄 출력

---

## 7. 사전 준비 / 전제 조건

- 개발 PC → 운영 서버 **SSH 키 기반 접속**(비밀번호 없이) 설정 권장
- 운영 서버에 `podman`(또는 `docker`) 명령 사용 권한
- 각 컨테이너에 `/app` 디렉터리 쓰기 가능 (없으면 스크립트가 `mkdir -p`)
- 컨테이너 내부에 `java`(temurin 17) 존재 — 이미지가 `eclipse-temurin:17-jdk`이므로 충족

---

## 8. 확정된 결정 사항

1. **JAR 소스**: ✅ `deploy-output/*-podman-ROOT.jar` 사용 (운영 프로파일)
2. **실행 위치**: ✅ 개발 PC에서 SSH(`scp` + `ssh`)로 운영 서버에 원격 실행
3. **컨테이너 기동 방식**: ✅ `podman exec`로 컨테이너 내부에서 `java -jar` 실행. JAR 경로/이름은 `/app/app.jar`로 통일
4. **재기동 정의**: ✅ 컨테이너는 그대로 두고, 내부 java 프로세스만 교체

### 남은 입력값 (스크립트 실행 시점에 채워야 함)
- 운영 서버 접속 정보: `REMOTE_HOST`, `REMOTE_USER`, `REMOTE_SSH_PORT` (SSH 키 기반 접속 권장)
- 빌드 포함 여부: 기본은 산출물만 배포, `--build` 플래그로 빌드 포함 선택 가능

---

## 9. 산출물

- `deploy-to-prod.sh` — 본 설계 기반 배포 스크립트
- (옵션) 모듈별 개별 배포 래퍼 또는 인자 기반 단일 스크립트 (단일 스크립트 권장)
