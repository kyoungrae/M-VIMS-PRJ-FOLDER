<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.login.password.PasswordResetMapper">

    <select id="COUNT_USER_BY_EMAIL" resultType="int">
        SELECT COUNT(*)
        FROM SYS_USER
        WHERE eml = #{email}
    </select>

    <update id="UPDATE_PASSWORD">
        UPDATE SYS_USER
        SET pswd = #{pwd},
            sys_mdfcn_dt = NOW()
        WHERE eml = #{email}
    </update>

    <!-- 비밀번호 정책 (PWD_POLICY) : cfg_key / cfg_val 목록
         공통 LowerCaseMapTypeHandler 가 키를 소문자로 변환하므로 원본 컬럼명(소문자)으로 조회 -->
    <select id="SELECT_PWD_POLICY" resultType="map">
        SELECT cfg_key, cfg_val
        FROM SYS_SITE_CFG
        WHERE cfg_grp_id = 'PWD_POLICY'
          AND use_yn = '1'
    </select>

</mapper>
